# Portal app models.
#
# The portal app serves as the public-facing grant opportunity listing and
# applicant interface. It does not define its own models -- it relies on
# grants.GrantProgram (and related models) for opportunity data.
